import { Colors as DiscordColors } from 'discord.js';

export enum Colors {
  SUCCESS = DiscordColors.Green,
  DANGER = DiscordColors.Red,
  WARNING = DiscordColors.Yellow,
  INFO = DiscordColors.Blurple,
}
